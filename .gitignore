# OS
.DS_Store
Thumbs.db

# Editor
.vscode/*
!.vscode/settings.json
.idea/

# Env / secrets
.env
.env.*
!.env.example

# Node (for the SDK packages added later)
node_modules/
npm-debug.log*
yarn-error.log*
pnpm-debug.log*
dist/
build/
*.tsbuildinfo
coverage/

# Python (for the face engine added later)
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/

# Real ID document samples — PII/biometric data, never commit (used locally
# only to derive document formats and zone maps; see Architecture Spec §9).
eID samples/
*-samples/
samples/
*.eid-sample.*

# Netlify
.netlify/
