# Sierrafy Admin Dashboard

Optional React + Vite + Tailwind monitoring SPA served alongside the gateway:
verification logs (no biometrics), error-rate charts, and API-key management.

**Status:** Week 0 scaffold — placeholder only. The Vite app is built in
Week 16 (M11); it joins the pnpm workspace at that point.
