# sierrafy_flutter (Flutter SDK)

Flutter SDK (`flutter_nfc_kit` + `dio`) porting the NFC verification path, with
an API surface matching the React Native SDK (`scanMRZ`, `readChip`, `verify`).

**Status:** Week 0 scaffold — placeholder only. Built in Week 15 (M10).
