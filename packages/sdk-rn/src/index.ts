/**
 * @sierrafy/sdk-react-native — mobile SDK with the full NFC verification path.
 *
 * `scanMRZ`, `readChip`, `verify` on `react-native-nfc-manager` (M9, Week 14).
 * Week 0 scaffold placeholder.
 */
export const PACKAGE_NAME = '@sierrafy/sdk-react-native';
