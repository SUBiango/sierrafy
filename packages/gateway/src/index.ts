/**
 * @sierrafy/gateway — self-hostable Express API gateway.
 *
 * Orchestrates validator → OCR → face-match → fraud → optional NFC behind auth
 * and rate limiting (M6, Week 10). Week 0 scaffold placeholder.
 */
export const PACKAGE_NAME = '@sierrafy/gateway';
