# sierrafy-face-engine

Standalone FastAPI face-match microservice (InsightFace `buffalo_l`) called by
the gateway over localhost. Memory-only processing — no biometric data is ever
written to disk.

**Status:** Week 0 scaffold — placeholder only. Built in Weeks 7–8 (M4).
