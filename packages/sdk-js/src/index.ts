/**
 * @sierrafy/sdk-js — JS/TS client wrapping the Sierrafy gateway.
 *
 * `Sierrafy` client with `verify`, `validateNin`, `ocr`, `faceMatch` (M7,
 * Week 11). Week 0 scaffold placeholder.
 */
export const PACKAGE_NAME = '@sierrafy/sdk-js';
